#include <stdio.h>
 
signed main(void)
{

	int n;scanf("%d",&n);
	int isprime=1;                //1=prime
  
	for(int i=2;i<n && isprime;++i)
	{
		if(n%i==0)        //被整除 不是質數
		{
			isprime=0;
		}
	}
 
	if(isprime)
		printf("YES\n");
	else
		printf("NO\n");
 
}
